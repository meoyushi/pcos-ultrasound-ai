# Python 3.11-3.13. Verified on 3.13.5 with tensorflow 2.21.0 / keras 3.15.1.
#
# The checked-in backend/models/pcos_efficientnet.h5 is a Keras 3 artifact
# (its embedded config uses batch_shape / DTypePolicy / quantization_config).
# It will NOT load under Keras 2, and ultrasound_service catches that failure
# and enters mock mode — so an unpinned resolve that lands on TF < 2.16
# silently disables the imaging model. Hence the floor below.
#
# These are minimum-version constraints, not exact pins. After a successful
# install on your target Python, run `pip freeze > requirements.lock.txt` and
# deploy from the lock file so builds are reproducible.

fastapi>=0.110
uvicorn[standard]>=0.29
pandas>=2.0
scikit-learn>=1.4
numpy>=1.26
openpyxl>=3.1
pillow>=10.3
python-multipart>=0.0.9
tensorflow>=2.16
